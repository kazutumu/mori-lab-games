# ミナと木陰の広場・操作試作

2026-09-14、参考画像の3D箱庭とコントローラーをミナで試す依頼に対する、独立したローカル試作。入口は `/mina-plaza`。既存12ゲームの一覧・本文・セーブ・公開版は変更しない。保存領域を使用せず、ページを開き直すと初期状態へ戻る。

## 今回の範囲

- 小さな広場、噴水、茶房、ベンチ、木陰。白いブラウス・青いスカート・結んだ髪の立体ミナは形状試作で、最終モデルではない。
- 左スティックでアナログ移動。カメラ基準の方向、斜め速度正規化、建物・噴水への衝突、指離し・キャンセル・フォーカス喪失による停止。
- 右の状況別行動。茶房で受け取り、読書家へ届ける二段階の小さな遊び。掲示板も読める。原作本文の改稿ではなく試作専用の短文。
- 景色をドラッグしてカメラ回転、ピンチ／ホイールで拡縮。キーボードはWASD／矢印とE／Enter。
- 既存の地面テクスチャを再利用。Three.jsの形状による3D試作。参照作品の画像・キャラクター・コードは流用しない。

## 性能と確認線

M1 iPadを最低想定。描画倍率上限1.5、影1024、花のインスタンシング。低フレームレート時は描画倍率1へ下げる。画面のfpsはその端末の実測値であり、M1での性能保証ではない。

実機M1・Safariとタッチ操作の確認は未実施。今回のブラウザ操作確認はユーザーへ質問済み。モデルの細部、歩行アニメーション、建物が人物を隠す場面のカメラ改善は、まず操作試作を触ってから判断する。公開・コミット・プッシュは行わない。

ロジック検証：`node --test tests/plaza-logic.test.mjs`。既存回帰検証は既存の3テストファイル。新しい依存関係なし。

2026-09-14 検証結果：新規3件＋既存29件の全32テスト成功。新規コードのESLint、全体build、差分確認成功。ローカルURL `http://localhost:5174/mina-plaza` はHTTP 200、既存ブラウザタブへ試作を開いた。ブラウザ実操作はまだ行っていない。

## 公開承認

2026-09-14、研究員がローカル試作を「とてもいい」と評価し、「公開してもらえる」と明示承認。上記のローカル限定方針から、確認済み試作の公開へ移行する。既存12カードは維持し、直接入口 `https://mori-lab-games.kazutumu0.chatgpt.site/mina-plaza` で公開する。新たなゲーム内容やモデルの変更は含めない。公開の成否と検証結果はhandoffへ記録する。
